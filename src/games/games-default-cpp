// Copy this file and rename to games.cpp
// Add additional include and conditional compilation to support multiple games
// Each game can be its own git repo located in the games folder. Strong-ARM gitignore is configured to ignore game code

#include "games.h"

#include "gameTemplate/gameTemplate.h"

void runGame()
{
	gameTemplate();
}
